import { createSlice } from '@reduxjs/toolkit';

const slice = createSlice({
  name: 'login',
  initialState: {},
  reducers: {},
});

export const {} = slice.actions;

export const {
  reducer,
} = slice;